include /mixins/button-hamburger.pug

header
  +hamburger({expanded: false})
  aside#top-menu(aria-hidden='true')
    div.top-menu-header
    nav
      ul
        each item in menu
          li
            a(href=item.link)= item.text
